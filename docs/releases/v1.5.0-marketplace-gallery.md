# v1.5.0 Marketplace gallery handoff

Publication status: **Not published**. Repository images and descriptor changes are preparation; no ZIP upload, public description edit, gallery replacement, release or tag is authorized in this task. The release workflow does not upload gallery images.

| Order | Repository asset | English gallery caption |
| --- | --- | --- |
| 1 | `docs/images/context-collection-overview.png` | Collect selections from multiple files beside your editor. Review capture paths, raw-code size and final-output size before copying. |
| 2 | `docs/images/context-collection-preview.png` | Keep before-and-after snapshots. Inspect original captured code and the exact final output with built-in snapshot labels. |
| 3 | `docs/images/context-collection-size-warning.png` | Confirm 262145 UTF-8 output bytes from one capture. Cancel keeps the clipboard unchanged; Copy Anyway copies the complete output. |
| 4–6 | The three existing Marketplace gallery entries, preserving their current relative order | Retain their existing captions until separately reviewed. |

The mapping between existing Marketplace entry IDs and the repository settings PNG has not been verified. Do not assume they are the same asset or delete/replace existing entries. The coordinator readiness checklist records the observed existing IDs. Keep the repository settings screenshot unchanged.

The first two real IDE PNGs are 1204×768. The required third image is the actual macOS native confirmation at 370×179: CUA exports the separate modal window alone and offers no parent-window-plus-modal capture. This required export variation preserves the confirmation without cropping, resizing or compositing. The 1204×768 `context-collection-size-warning-inline.png` is supplemental guide evidence, not a substitute gallery confirmation. All final captures use source `4de4caf6e097d5a54a5ec52e62b4d26c935d5a9d`; hashes and reproduction are in the capture guide.

After separate publication approval, inspect Marketplace General Information → Preserve changes from UI. Coordinator's read-only observation on 2026-09-04 found **Use data from plugin.xml**, three existing gallery entries, and moderation pending. Recheck at publication time: this observation does not establish future configuration or public text. If an independently edited page description is effective, update that source to match the approved descriptor. Verify the public page after moderation; uploading an archive alone is not proof that the live description or gallery changed.

Use the approved canonical release ZIP from GitHub Actions. Upload the three real English IDE PNGs in the order above, preserve all three existing entries in their current relative order after the new images, add captions, and record the exact public page URL, timestamp and observed ordering/description. Respect any current moderation restrictions. Do not mark this handoff complete until public rendering is verified.

## Release checklist contribution

- [ ] Coordinator verifies the final integrated implementation SHA, independent approval and required CI.
- [ ] Confirm the three PNGs, capture guide and five localized READMEs match the approved behavior.
- [ ] Separately approve version bump/tag, GitHub Release and canonical Marketplace ZIP publication.
- [ ] Recheck the effective description source and publish the approved description.
- [ ] Upload and order gallery images with captions after authorization.
- [ ] Verify public description and images after moderation; record evidence in the coordinator's readiness checklist.

See [capture evidence](v1.5.0-context-collection-capture-guide.md). Local development version remains 1.4.1; a validation ZIP is not a canonical release.
