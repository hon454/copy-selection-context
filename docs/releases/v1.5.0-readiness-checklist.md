# v1.5.0 readiness and publication checklist

Status: implementation in progress; publication is not authorized yet.
Coordination evidence: [implementation progress](../plans/v1.5.0-implementation-progress.md).

## Repository readiness

- [x] #73 merged via PR #90 at e2df16cba5d6df07d6cdea77480dccf5f613e6bf after independent APPROVE of head 1b14019ee0451e0783f06508798c05df1a7462a2 and passing required local/CI gates.
- [x] #75 merged via PR #91 at462d387de118c4be3cd328dbdc8bfc1dd9ee33ce after exact-head independent APPROVE,393tests/fullgates and CI33856511805 SUCCESS.
- [ ] #74 merged on #75, including actual IDE screenshots and all listing materials.
- [ ] Final main SHA recorded with all component merge SHAs and review heads.
- [ ] Final integrated detekt, allTests, Kover XML/HTML, project/structure verification, Plugin Verifier and buildPlugin pass.
- [ ] Final integrated manual verification matrix completed with evidence and no unresolved blocking findings.
- [ ] Local verification ZIP path, SHA-256, plugin version and source SHA recorded. This ZIP is not the canonical release artifact.
- [ ] Five READMEs have synchronized content/structure and localized captions/alt text for the same three English screenshots.
- [ ] plugin.xml explains collection workflow, session lifetime, frozen captures, history exclusion and fixed byte limits.
- [ ] CHANGELOG and architecture/pattern guidance reflect final behavior.
- [ ] Every finished owned implementation/review task has an evidence/handoff record and verified archive result; tasks awaiting reuse remain open.

## Real IDE evidence

Record actual plugin version/SHA, IDE name/build, Java runtime, OS, locale, theme, viewport and date. Use a separate test sandbox/project with non-sensitive samples. One task at a time owns IDE/browser interactions. Preserve the user's unrelated sessions.

| Asset | Required view | Readiness |
| --- | --- | --- |
| `docs/images/context-collection-overview.png` | Multiple files, captured path/ranges, include-code control, raw and output bytes | Pending actual capture |
| `docs/images/context-collection-preview.png` | Captured original and final output, same-location versions with stable snapshot labels | Pending actual capture |
| `docs/images/context-collection-size-warning.png` | Actual confirmation above 256 KiB with matching exact output bytes and item count | Pending actual capture |

Target one readable 1280×800 English UI viewport, adjusting only when necessary and recording any variation. Capture guide and sample project paths will be supplied by #74. Validate screenshot contents against the running plugin and payload; mockups/generated images do not satisfy readiness.

## Marketplace material order

Proposed gallery order and captions to finalize against the actual captures:

1. Overview — “Collect selections from multiple files in one project session.”
2. Preview — “Review captured code and the exact output before copying.”
3. Size warning — “Confirm large outputs with an exact UTF-8 size and item count.”

Keep the existing settings image unless that screen changes; record its final placement. Repository assets and a plugin ZIP do not establish public gallery publication. Final captions must match actual UI; the English image set is shared across localized README text.

Official guidance checked on 2026-09-04: [listing guidance](https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html) identifies General Information as the description-source control and Media as the separate gallery editor. Page-edited descriptions can persist independently of the archive. Its general screenshot advice recommends at least 1200×760, consistent aspect ratios, default IDE theme and readable text. The agreed 1280×800 target meets that advice. [Approval guidance](https://plugins.jetbrains.com/docs/marketplace/jetbrains-marketplace-approval-guidelines.html) requires review of new versions; do not infer public availability from upload completion. These sources do not establish the account's selected description source; the separate authenticated observation below does.

### Observed listing baseline — 2026-09-04

Coordinator inspected the authenticated [listing management page](https://plugins.jetbrains.com/plugin/30262-copy-selection-context/edit) in a dedicated Chrome tab, using read-only navigation. General Information → Preserve changes from UI displays **Use data from plugin.xml**. The effective configured description source is therefore the uploaded archive at this observation, rather than a persistent page-level description override. Recheck this control before the approved v1.5.0 upload, and verify the actual public text after publication.

The page also displayed a moderation-pending banner and disabled Edit Section buttons. This does not block repository readiness and must not be interpreted as approval for the future v1.5.0 release. The Media section contains three existing screenshot entries; none was changed. Their identities are `screenshot_14e0a2c4-cb20-4925-a6ce-99933b5a2561`, `screenshot_ee87cfdc-0cc7-40d6-87b4-00d4f6bf9e94`, and `screenshot_24239eaa-4469-4f0e-bad9-f03de2c039c3`. Review the existing images alongside the new approved assets before finalizing gallery placement; do not silently remove them. The dedicated inspection tab was closed; no account settings, text, media or package upload was changed.

## Publication approval boundary

Prepare a concrete approval request listing the final SHA/version, release notes, canonical workflow to run, expected public changes, Marketplace description source and all gallery images. Keep this coordinator open while waiting. No tag push, GitHub Release, Marketplace upload, public description override or gallery edit is allowed before that approval.

After approval, follow existing release policy without modification:

- [ ] Prepare/verify the 1.5.0 version and changelog using the repository's release process; tag version must match build.gradle.kts.
- [ ] Push the approved v1.5.0 tag; record tag SHA and release run URL.
- [ ] Confirm release workflow gates pass and identify its canonical GitHub Actions ZIP.
- [ ] Record whether canonical ZIP signing/signature verification ran. Marketplace publication requires non-empty PUBLISH_TOKEN, CERTIFICATE_CHAIN and PRIVATE_KEY; do not inspect or disclose their values.
- [ ] Verify SHA256SUMS and GitHub provenance for that exact ZIP; record the GitHub Release URL.
- [ ] Confirm the workflow uploads the same canonical ZIP to Marketplace when its configured conditions permit; record update/version/review status.
- [ ] Inspect the listing's effective description source: archive plugin.xml or independently edited page-level override. Apply the approved text to the effective source and verify the live result. A successful ZIP upload alone is insufficient.
- [ ] Upload the approved gallery images in the agreed order through supported Marketplace controls and verify the public gallery after review. The current workflow has no screenshot-upload step.
- [ ] Record public description/gallery URLs or screenshots and actual verification dates separately from package publication.
- [ ] Close milestone 6 only after publication obligations are complete.
- [ ] Record final task IDs, PRs, SHAs, verification, artifacts and remaining handoff; report results, then archive the coordinator only when no unresolved work remains.

If the user explicitly ends at readiness, leave all unexecuted publication steps as handoff and record that instruction before archiving. Store review waiting is a publication state, not repeated implementation verification.
