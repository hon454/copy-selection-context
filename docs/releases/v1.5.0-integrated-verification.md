# v1.5.0 integrated verification record

Execution status: final integrated automated gates and the recorded functional IDE matrix are complete; independent final evidence acceptance and a consistent full-frame warning screenshot remain pending. Follow the [manual validation guide](v1.5.0-manual-validation-guide.md) and keep automated evidence distinct from UI observations.

Final integrated main SHA: `b70f8e27227f278adee6a30d60982542ad32b660`. Executed coordination merge: `a85f435081f1161bf13894c339c77913c9efe45f`; `git diff --exit-code origin/main -- src build.gradle.kts settings.gradle.kts gradle.properties gradle gradlew gradlew.bat` passed before execution. No source/build/wrapper differences from main.
Local verification ZIP: `build/evidence/integrated-b70f8e2/copy-selection-context-1.4.1.zip`, version1.4.1,365662bytes, SHA256 `2c6c8efacc5c4275669433a0bfda2ca71f820470069c8bed5901d978c70893e1`. This matches the independently approved/captured source ZIP and is not a canonical release artifact.
Automated environment: macOS26.6.2 arm64, JDK21 at `/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`, Gradle9.7.1. Actual final IDE observations: coordinator resumed on2026-09-04 at approximately22:18–22:35 KST in the isolated official IntelliJ IDEA CE243.21565.193, English/Dark, with the same verified ZIP. After a renewed lock interruption, access resumed and the subsequent checks below succeeded. The settings-combo accessibility interruption was recovered after user closure and normal app restarts. Subsequent Path:Line, legacy history and combined-warning observations are recorded below. No force termination or security bypass was used.
Operator task: `01a06b69-7322-7c90-a7ef-3997e89c1ceb`. #74 owner explicitly handed GUI back after closing its IDE/browser/server. Coordinator retains exclusive UI ownership for final checks; the successful observations below were executed after handback.
Reproducible sample and screenshot guide: [capture guide](v1.5.0-context-collection-capture-guide.md), `docs/samples/context-collection/`; disposable final boundary inputs at `build/ide-demo/collection-boundaries/manifest.json`. Generated inputs alone are not execution evidence.

## Automated gates

Run with JDK 21 and the actual host's wrapper. Preserve complete logs and test reports. Record exact commands and exit codes, not only a general claim of success.

| Gate | Result | Evidence |
| --- | --- | --- |
| detekt | PASS | `build/evidence/integrated-b70f8e2/all-gates.log`, `detekt/` |
| allTests including isolated platformTest | PASS:360pure +42platform;0failures/errors/skips | `build/evidence/integrated-b70f8e2/test-results/` |
| koverXmlReport and koverHtmlReport | PASS | `build/evidence/integrated-b70f8e2/kover/` |
| verifyPluginProjectConfiguration | PASS | Full gate log |
| verifyPluginStructure | PASS | Full gate log |
| verifyPlugin on current configured IDE set | PASS: IC243.28141.41,251.29188.72,252.28539.97 | Full gate exit0 and `pluginVerifier/`; not verdict text alone |
| buildPlugin and local ZIP integrity | PASS | Preserved ZIP and `summary.json` |

Actual command:

```sh
JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew detekt allTests koverXmlReport koverHtmlReport verifyPluginProjectConfiguration verifyPluginStructure verifyPlugin buildPlugin --rerun-tasks --console=plain
```

Exit0, BUILD SUCCESSFUL in1m13s,31tasks all executed. Full log and XML/reports are preserved under `build/evidence/integrated-b70f8e2/`. This is a new coordinator execution after main merge; it does not substitute for the actual IDE scenarios below.

## Integrated verification matrix

Actual IDE checkpoints are identified explicitly. Injected failures, reentrancy and delayed order use the final registered fixtures, as required by the manual guide; no operating-system fault injection or uncontrolled GUI race is claimed. Earlier owner visual/locale evidence is attributed separately and applies to the identical final production ZIP.

| Scenario | Expected observation | Result / evidence |
| --- | --- | --- |
| Multiple files and carets | Captured exact unsaved text in ascending range order, inclusive end handling, current-line fallback | PASS: action3/5/6/7 AX; unsaved changed text, two carets ordered line3 then4, selection ending3:1 stored1–2, no-selection line5 captured; two files,6items/120raw bytes |
| Changed same-location capture | Old text remains; recapture adds new version; stable sequence/time across reorder | PASS: action3 AX retains10 and adds30 with distinct UTC millisecond labels;13 preserves #1 and its timestamp after moving it below #2 |
| Relative/absolute preference | Unchanged recapture is duplicate with unchanged original display; changed code gets new display but same conflict group | PASS: action2/3 AX; preference change retains1item/16raw and original relative display; changed capture uses absolute display in the same snapshot group |
| Rename, move, delete, recreate | Captured output fixed; textual source status independent; new file does not impersonate old identity | PASS:07–10 AX/JPG;6items/120raw/output940 retained through actual rename/move/delete; recreation does not clear Source unavailable; normal clipboard paste yields940UTF-8 bytes |
| Capacity batch | Exact 100 items/256 KiB per item/2 MiB raw allowed; above-limit batch rejected atomically after dedupe | PASS:24/25 exact262144 and rejected262145;26/27 exact2097152 and rejected extra1;37–42 exact100 distinct11byte ranges,101-range rejection and99existing+2new rejection retaining99/1089raw |
| Keyboard remove/reorder | Predictable selected row and focus; edge moves disabled; Delete limited to list | PASS:13 move preserves ID/time;16/17 viewer Delete/type leaves content and rows unchanged;18 final-viewer copy pastes292ASCII chars/8LF;19/20 last-edge disable and list Delete selects surviving #7 with list focus;41 removal selects next #20 |
| Clear cancellation and confirmation | Cancel retains; confirmed count removed; list change invalidates old confirmation | Actual ordinary cancel/confirm PASS:21–23 confirms1item, cancel retains1, confirm yields0 and disabled Copy All. Nested content-revision change is covered separately by the final registered fixture |
| Code option and settings | Session include-code independent of single-copy; format/template/trim invalidates bytes until current result ready | PASS with automated transient-state coverage: code toggle changes output while captures persist;44/45 substitutions and trim change288→285bytes while raw8 remains. One-shot setting stays false in persisted state. 47/50/54 verify Path:Line and actual single-copy code-option independence; transient Calculating is not claimed observed |
| Built-in snapshots | Same-location captures receive UTC millisecond labels; one-shot/unrelated items preserve formatting | PASS: action3/5/7 AX verifies Claude snapshot labels and unrelated Python items without labels; 47/50/54 additionally verify Path:Line labels and unlabelled one-shot output |
| Templates and blank output | Existing substitutions preserved; no forced labels/code; small conflicts still warn; blank item blocks copy | PASS:43 normal alpha plus whitespace capture blocks all and identifies #119;44 substitutes all six tokens,45 trims only output;42 code-off custom output blocks at #18. 55 verifies the12byte custom snapshot-conflict warning |
| Final size boundaries | Exactly 256 KiB has no size warning; above warns; exactly 4 MiB allowed with confirmation; above blocked | PASS:28 exact262144 copies without dialog;29/30 above262145 dialog/defaultCancel and retained262144 clipboard;32 Copy Anyway normal paste262145chars/1LF;33/34 exact4194304 confirmation and4194304char paste;35 template+oneASCII blocks Copy All above4MiB |
| Combined warnings | Exact same payload/count/bytes for size and snapshot/reference reasons; one dialog; cancel retains everything | PASS:11/12 confirms small reference-only738byte/6item warning and Cancel preserves prior940byte clipboard;29–32 independently verifies size-only warning. 56 verifies one combined262158byte/3item dialog.75–77 subsequently verify combined262162byte/2item confirmation, cancel preserving prior144character normalized paste, and approved262162ASCII-character/2LF complete paste |
| Prepared result invalidation | Content/settings change before dispatch or in dialog prevents stale copy; source-only change remains valid | Automated PASS: final registered output fixture with controlled queues and injected confirmation mutation; exact mapping below. Actual source-only retention/paste additionally passes07–10. No manual concurrent-edit timing claim |
| No active editor | Open empty tool window and copy valid collection through Find Action; no dummy editor | PASS:01/03–05 AX/JPG; empty opening, actual tab closure retains1item, Find Action copy succeeds; subsequent normal paste yields17characters/1LF matching17bytes |
| Collection side effects | One clipboard write, no history/gutter; enabled analytics/review once; notification preference honored | Actual history preservation49/52 and analytics0→1 (claude1/kotlin1)61/62; status re-copy stays1 in64. Notification setting remains false. Exact one-write/gutter/review/optional-effect policy is Automated PASS in final output/publisher fixtures; no actual review prompt or injected OS failure claimed |
| Optional feedback failures | No clipboard retry or duplicated counters; later optional effects attempted; Published includes failure metadata | Automated PASS: final CopyResultPublisherTest injected failures/reentrancy and policy effect lists. No destructive real OS fault injection needed or claimed |
| Hide and reopen UI | Retained collection; no auto-open/focus steal on addition; resource/listener disposal verified | PASS with separate automated disposal coverage:02/03 AX confirms hidden Add retains editor selection and does not open panel; opening after closing editor retains capture. Resource disposal remains separately covered by automated fixtures |
| Project close and reopen | Captures discarded and include-code reset true; plugin settings contain no collection snapshots | PASS:65 code-off/1item,66 actual File→Close Project/Welcome,67 same-process reopen has0items and checked Include code. New capture uses code by default. persisted-settings-observation.xml/json contains no captures or session code option; latest UI flush is not inferred |
| Cross-project managed copy ordering | Controlled delayed A completion cannot overwrite newer B through each managed path; failed/cancelled B never revives A | Actual two-window latest-result flow70–73: A collection→B absolute path, then B collection→A status re-copy. A retains2items/raw31 and B1item/raw16. Automated final controlled-queue/publisher fixtures cover delayed/cancelled/failed/disposed/reentrant variants, with audit limits below |
| Existing copy regressions | Standard, code, relative/absolute, permalink, history and status re-copy preserve documented effects | Actual PASS: standard50,history53,status63/64,permalink60,relative69,absolute71,explicit code74. Kotlin paste normalizes Claude margin spaces; these legacy screenshots establish paths/ranges/code, not raw clipboard byte identity. Final action/publisher fixtures assert policy effects |
| Accessibility and locale | Textual statuses, meaningful names, reachable viewers/actions, narrow-layout usability; five bundle parity | PASS: coordinator keyboard/viewer/status AX16–23 plus independently reviewed owner English/Japanese long-path,520/280width,light/dark evidence in pr92-39edac0/owner-ui on identical ZIP; final descriptor/document/bundle tests and panel fixtures pass. Owner evidence is not relabelled as a new coordinator capture |
| Documentation and screenshots | Three real English images accurate and readable; five README image paths/rendering/mobile width verified | Repository source/assets/five README desktop and375px render review PASS at39edac0, identical ZIP. Actual modal370×179 versus other1204×768 exports remains an unresolved consistent-frame/Marketplace suitability limitation; no full readiness claim |

Use deterministic barriers/fakes for interleaving tests rather than timing sleeps. Distinguish automated injected failure evidence from actual UI operation. Capture bytes include headers, annotations, fences and separators. Source-only changes do not invalidate content snapshots. OS/external clipboard history is outside the plugin's session-only retention contract.

## Deterministic evidence mapping

These suites were inspected in the final-run XML and independent source audit recorded below. Their automated assertions remain distinct from the actual IDE observations above.

| Contract | Existing suite / specific test |
| --- | --- |
| Atomic count/raw limits, duplicate order, stable IDs, malformed UTF-16 | `ContextCollectionStoreTest` |
| Unsaved/multicaret capture, path preference, source identity and session disposal | `ContextCollectionFixtureTest` |
| Exact output boundaries, conflict labels, custom semantics and empty-fragment cancellation | `ContextCollectionFormatterTest` |
| Settings/content invalidation versus source-only updates | `ContextCollectionOutputFixtureTest.testCommittedSettingsInvalidationDiscardsOldResultsAndSourceOnlyDoesNotCalculate`, `testEveryFinalPathRejectsChangedContentCommittedSettingsAndDefensiveActualTuple` |
| Confirmation reentrancy, combined reasons, no-editor copy and history/gutter preservation | `ContextCollectionOutputFixtureTest.testWarningCancelAndConfirmedPayloadRetainCollectionAndCombineReasonsOnce`, `testSmallConflictStillConfirmsAndMutationInsideConfirmationCannotPublish`, `testNoEditorCopyRetainsCapturedDeletedSourceAndExistingHistoryAndGutter` |
| Cross-project request order, cancellation/failure/disposal and feedback reentrancy | `CopyResultPublisherTest` and `ContextCollectionOutputFixtureTest.testTwoProjectsRecopyAndDelayedPublisherPathsUseOneSequenceWithoutCrossProjectEffects` |
| Background service initialization and delayed settings signals | `ContextCollectionOutputFixtureTest.testBackgroundReadActionLoadRestoresSynchronouslyWithoutWaitingForEdtAndInvalidatesBeforeSignalDelivery` |
| Tool-window selection, Delete scope, disposal and stale viewer installation | Final #74 `ContextCollectionPanelFixtureTest` plus retained independent reviewer probes |

For each final result, record the exact executed head, test count/failures/errors/skips and report path. A selected-suite preliminary pass does not substitute for final allTests or actual UI operation.

## Automated evidence limits

The independent audit confirms counts, hashes, actual rerun and source applicability. No-editor fixture coverage uses a DataContext without EDITOR and direct command execution; it does not establish closing actual tabs and invoking Find Action. The two-project fixture uses actual project services and controlled queues, with some B operations called directly through publisher APIs. Latch-based tests verify the observed final ordering but do not prove that the other worker reached monitor acquisition before release; do not describe them as exhaustive or deterministically covering every race. No synchronization defect was found. The two earlier reviewer-only viewer probes are separate prior evidence and are not included in402.

Main push CI33865290654 completed SUCCESS on b70f8e2, confirmed by `gh run watch --exit-status` exit0 and final GitHub JSON. Full log/status/watch output are preserved beside the local gate evidence. PR-head CI33864526229 is a separate successful run.

## Coordinator actual evidence — resumed session

All files are under `build/evidence/integrated-b70f8e2/manual/`; The initial independently audited manifest covered28 original AX/JPG files. The updated `manifest.json` records105 evidence files including subsequent AX/JPG checkpoints and a diagnostic thread dump; all105 entries passed the independent boundary-evidence audit. Later checkpoints46 onward await final audit. AX-only action records preserve intermediate state, and image/AX pairs preserve visible checkpoints. The actual normal paste in05 contains17ASCII characters including oneLF;10 contains937Unicode characters/33LF, including three middledots, matching940UTF-8 bytes. The recreated disposable source is only a paste destination, not a new identity for the old capture.

Observed settings transitions: Custom `{code}` plus LF to Claude Code, relative to absolute, then session Include code off. Existing capture display stayed fixed. Changing the format through a native combo popup caused a CUA accessibility/screenshot access issue; normal app quit/relaunch restored access. Subsequently keyboard focus plus Home selected Claude successfully. This is recorded as a tooling limitation, not a plugin defect. The restart evidence does not establish an include-code false-to-true reset because it occurred before that option was disabled.

Checkpoint11 shows the actual reference-only confirmation at738bytes/6items, with Cancel as the visible default. Return dismissed it and retained the collection in the live observation. That paste check initially stopped at Mac lock, then succeeded in12 on the next continuation:937characters/33LF with three middledots confirms the prior940byte payload, while current reference-only output remains738bytes. The six captures were later cleared during the next test sequence. Owner's earlier large-warning cancellation evidence remains separate.

### Subsequent keyboard, capacity and output checks

Checkpoints13–45 and `boundary-add-1-ax.txt` through7 record the actual subsequent checks. All original evidence is retained. Three filenames were corrected to reflect intermediate states:14 is `14-clear-keyboard-transition` (a Clear All dialog reached during an attempted keyboard/viewer sequence, not a successful viewer test);31 is `31-approved-copy-before-editor-focus` (old selection before actual paste);36 is `36-column-mode-search` (toggle search still open, not100carets). Use16–18,32 and37 respectively for the completed observations. Do not infer outcomes from attempts or stale focus.

The count fixture was first selected in column mode as100 independent11character ranges;37/38 show100carets and100items/1100raw. Extending to101ranges rejects the batch at100. After removing #19,99items/1089raw remain; the operator repeated Add with101ranges (99duplicates plus2new), and41/42 preserve99items/1089raw/101carets/selected#20. The retry and atomic rejection attribution depend on the operator action record:42 contains no visible rejection notification, and virtualized AX rows do not prove all99snapshots individually unchanged. The column-mode search remains open after toggling; Escape closed it before actual range selection. General selection mode was restored before the next samples. Capture numbering advances after Clear All (#7/#8 and later #118/#119), rather than reusing removed IDs.

The eight prepared262144byte files were added individually through the actual IDE, reaching2097152raw. The extra single-byte capture was rejected with the total-limit reason. Input fixture hashes describe their initial prepared contents; subsequent clipboard tests deliberately mutate only the disposable editor. They are not claimed to remain unchanged on disk. The final4MiB paste is proven by the actual4194304character ASCII selection count; a truncated accessibility value is not treated as a complete4MiB byte extraction.

For template substitution/trim, the disposable three-line source is `alpha`, three spaces, and `omega`; captured #118 is alpha and #119 is the spaces. Code-on `{code}` blocks at #119 even though #118 is nonblank. `{filename}|{path}|{line}|{range}|{lang}|[{code}]` produces288bytes with `[   ]`; enabling trimming produces285bytes with `[]`, retaining2items/8raw. Code-off `{code}` separately identifies the first blank item. Single-copy Include code remains defaultfalse in CopySelectionPlugin.xml, distinct from the collection toggle; checkpoints49–54 subsequently verify actual legacy one-shot independence and history behavior.

### Historical UI access and capture limitations

During29 the supported macOS Screenshot shortcut and native Screenshot app were attempted for a real full-frame confirmation. The app lookup timed out; no usable full-frame image was produced. The actual modal JPG is preserved. No synthetic image, screenshot CLI, private API or security workaround was used; Marketplace export suitability remains unverified.

After45, the Settings combo caused title-only accessibility and unavailable screenshots. At that diagnosis, owned PID29484/session35456 was alive and its saved read-only thread dump showed the EDT waiting in the modal event loop, not an observed deadlock. The user subsequently closed Settings. Normal Cmd+Q/confirmation returned exit0; an ordinary sandbox relaunch failed with UnixDomainSocket EPERM/exit134 (saved under runtime-recovery), and the authorized isolated launch restored access. After applying Path:Line through keyboard navigation, another normal exit0/relaunch restored the same popup-related tooling limitation. Checkpoint46 confirms an empty collection after restart;47/54 verify actual Path:Line output. These historical diagnostics are not current process-state claims.

### Subsequent format, history and combined-warning observations

Checkpoints47–56 were captured in the same isolated IDE/ZIP after normal restart. Two frozen captures at the same location contain alpha and bravo, IDs1/2, UTC2026-09-04T14:25:31.374Z and14:25:41.601Z, raw10. Path:Line output is352UTF-8bytes including two snapshot middledots;54 normal paste selects350Unicode characters/10LF. One-shot Include code remains false: the real main shortcut produces the116ASCII-character absolute path:line output in50, without code or snapshot labels. Collection Include code stays enabled and both captures remain unchanged.

49 shows one standard-copy history entry;52 shows exactly the same entry after collection Copy All.53 actual history re-copy pastes the same116characters.48 is only the history action search, and51 is a pre-popup transition, not a history-result proof. Empty history produced no popup before the standard copy, consistent with the source early return; that absence alone is not treated as independent state extraction.

55 shows a real small custom-template snapshot warning: `{code}` renders12bytes from alpha/bravo, with no inserted capture labels, and warns despite being below256KiB. After adding one262144ASCII-a capture from the same source location,56 shows one combined confirmation for3items/262158bytes containing both the missing snapshot-label reason and the size reason. No second dialog is inferred or substituted. The user then closed the IDE, so56 had no paste follow-up;75–77 subsequently completed a fresh combined-warning cancel/copy sequence.

Independent bounded audit of12–45 and seven boundary-add AX files is PASS, report at `build/evidence/integrated-b70f8e2/independent-review/manual-boundaries-20260904/`. All105manifest entries match size/SHA256. Exact4194305blocking attribution in35 uses the recorded16×`{code}` plus oneASCII template input; the reviewer independently confirms above4MiB/disabled Copy All, not a complete output extraction. The large paste evidence is selection count, not a full-payload hash. The audit does not approve overall readiness.

## Final acceptance

Blocking findings: no outstanding repository source/assets/docs finding at approved39edac0; recorded actual functional flows have no observed defect and await final independent evidence acceptance. The settings-popup tooling interruption is recovered. Native modal Marketplace upload suitability remains unverified.
Independent review task: `01a06b7f-2626-7500-bad9-496d8b62b97e`; PR92 head39edac0 repository APPROVE. Independent read-only audit PASS for automated gates and bounded regression assertions on mainb70f8e2/executiona85f435. Report: `build/evidence/integrated-b70f8e2/independent-review/review.md`. Reviewer independently parsed402 XML cases and mapped97 cases in9 relevant suites to source assertions; no extra test execution is implied.
Final integrated decision: not ready.
Publication: unexecuted, separate approval required.


## Final manual continuation after user restart

The user closed the test IDE while the full-frame screenshot request was pending and asked the coordinator to retry. Session26819 ended normally/exit0. A fresh isolated launch reproduced the actual262145byte warning in58, but supported native Screenshot app lookup again timed out. The capture attempt was ended and a subsequent normal quit/relaunch restored UI access. No usable full-frame capture was produced and no security restriction was bypassed.57 shows session captures discarded after the user's restart. No checkpoint59 exists: the attempted code toggle/save failed before evidence could be recorded.

The user relayed through side task01a06cb3-b585-7ad0-b383-e6e0a2e3097d that Cmd+Shift+A was opening macOS manual Terminal windows (`No manual entry for Copy GitHub`). Coordinator stopped that shortcut immediately and uses Help→Find Action with direct result selection instead. A supported Terminal lookup to close only attributable automation windows was denied by the UI tool's app safety restriction. No terminal window was inspected/closed through another route and cleanup is not claimed complete. The side task is not an owned implementation/review task and must not be archived here.

A second disposable Git project was prepared locally at collection-demo-b with commit4ec857b08b071fe8d11181d20c64e50cd00ffbbf and originhttps://github.com/hon454/copy-selection-context.git. It was never pushed; the117character actual permalink in60 correctly uses this local commit and src/demo/Timeout.kt#L3, without implying that the fixture commit exists on GitHub. The actual project open prompt changed through user interaction before the coordinator could change the broad-trust checkbox; no coordinator blanket trust action is claimed.

61/62 actual local analytics rises from0 to1, with claude1/kotlin1, after exactly one collection Copy All.63 status re-copy reproduces path and code;64 keeps total1. The145byte Claude formatter payload has two intentional margin spaces; the Kotlin paste is143ASCII characters/3LF, consistent with paste normalization. Owner read-only source/evidence inspection confirms status re-copy forwards the stored string without trim.69 relative path paste is24characters,71 absolute path113,73 A status payload289versus prepared293, and74 explicit code144versus formatter146. These are content/path/range regression observations, not byte-exact clipboard dumps. Path:Line54 and custom77 have separate exact-byte evidence.68 is an excluded search transition, not a successful paste.

65 shows B Include code unchecked with1item/16raw;66 File→Close Project reaches Welcome;67 reopens B in the same process with0items and checked Include code. A subsequent new capture retains16raw and emits code, confirming the reset independently of one-shot Include code false. Persisted settings names contain no collection data or session include-code option; the captured XML may precede the last UI flush and is not a current complete settings snapshot.

70–73 use two actual project windows in the same process, switched through Window menu. A has2captures/raw31 from retained editor carets; B has1/raw16. A collection copy followed by B absolute copy yields B's path, then B collection followed by A status re-copy yields A's two captured code fragments. Each project retains its own collection. Delayed order/failure/cancellation remains the separately audited controlled fixture evidence, not an uncontrolled UI race.74 verifies explicit code copy of val timeout =30.

75 has2same-location snapshots (val timeout =10 and262144ASCII a), raw262160, custom `{code}` output262162. One actual dialog contains both missing automatic snapshot-label and above256KiB reasons. Cancel then normal paste in76 exactly reproduces74's144character/3LF prior normalized result; collection stays2/raw262160. Copy Anyway then normal paste in77 selects262162ASCII characters/2LF, retaining both items. This closes the interrupted combined-warning paste case from56.
